# Binaries
bin/
bin/photod
bin/photo
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test artifacts
*.test
coverage.out
coverage.html

# Go build cache (not typically committed, but just in case)
*.out

# Dependency vendor directory (if ever used)
vendor/

# SQLite databases
*.db
*.db-shm
*.db-wal

# Server config (contains JWT secret)
# Client config (contains auth token)
# Both are kept in platform config dirs, not the repo, but guard anyway
config.json

# OS artifacts
.DS_Store
.DS_Store?
._*
.Spotlight-V7
.Trashes
Thumbs.db

# Editor artifacts
.vscode/
.idea/
*.swp
*.swo
*~
